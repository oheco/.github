# 参与 OpenHarmony 开源生态建设

oheco 欢迎开发者和开源维护者将开源软件移植到 OpenHarmony 与 HarmonyOS，并持续维护适配成果。我们希望每个接入的项目都有明确的上游版本、负责的维护者，以及可供用户下载和安装的发行包。

## 1. 确认参与意愿

参与者需要是**开发者和开源维护者**，能够开展项目适配，并愿意承担适配后的维护工作，包括处理问题、跟进上游版本和修复适配代码。

项目维护者负责源码适配、验证和日常维护；组织维护者负责创建仓库、授予开发权限，以及统一打包、发布和更新软件目录。

## 2. 选定项目和 Release 版本

选择一个希望适配的开源项目，并确定一个具体的上游 **Release 版本**作为起点。申请中请提供上游仓库和 Release 链接，明确对应分支或标签，以便组织维护者取得同一版本的源码。

提交申请前，可先查看 [oheco 的现有仓库](https://github.com/orgs/oheco/repositories)及[适配申请 Issues](https://github.com/oheco/.github/issues)，了解该项目是否已有维护者或正在适配。

## 3. 在 `.github` 仓库提交 Issue

到 **[oheco/.github 的 Issues](https://github.com/oheco/.github/issues/new)** 提交适配与维护申请，说明你希望适配哪个项目、基于哪个 Release，以及你的适配计划和维护意愿。

建议标题使用：`[适配申请] 项目名 上游 Release 版本`。正文可参考：

```markdown
## 项目信息

- 项目名称：
- 上游仓库地址：
- 开源许可证：
- 选定的 Release 版本及链接：
- 对应的分支或标签：

## 适配计划

- 目标系统及版本（OpenHarmony / HarmonyOS）：
- 目标架构：
- 计划支持的功能或命令：
- 已有适配进展、主要依赖或待解决问题：

## 维护意愿

- 我的 GitHub 用户名：
- 相关开发与开源维护经验：
- 我愿意负责该项目的鸿蒙适配及后续维护，包括问题处理和版本跟进。
```

组织维护者为 **Guo Wei（[@kdada](https://github.com/kdada)）**。可以在申请 Issue 中 `@kdada`，便于跟进。

## 4. 由组织维护者创建仓库并授权

组织维护者确认申请后，会在 `oheco` 组织下创建一个**与上游开源项目同名的仓库**。

组织维护者只拉取选定 Release 对应的版本分支作为适配主干，**不会同步上游的全部分支**。如果 Release 以标签标记，则以该标签对应的提交建立适配主干。所选 Release 的源码是后续适配工作的基线。

仓库准备完成后，组织维护者会授予申请者该仓库的**开发权限**，由申请者作为项目维护者开展工作。

## 5. 开展适配，完成后通知打包

开发者基于组织创建的仓库进行适配，在目标 OpenHarmony 或 HarmonyOS 环境中完成构建和功能验证，并将适配代码与使用说明提交到该仓库。

为便于组织维护者复现和打包，请在仓库中说明：

- 上游 Release 版本、适配范围，以及已验证的系统版本和架构。
- 构建步骤、工具链和依赖要求，以及必要的构建脚本。
- 主要功能的验证方式与结果、已知限制和运行依赖。
- 需要发布的程序及目录布局，并保留上游许可证和第三方许可说明。

适配完成后，在原申请 Issue 中通知组织维护者 `@kdada`，提供适配仓库链接、待打包的提交或标签，以及构建和验证说明，申请打包。

## 6. 由组织维护者打包、发布并收录

组织维护者基于适配完成的源码进行打包，处理目标系统需要的二进制签名与发行包校验，然后在该项目的适配仓库中创建 **GitHub Release** 并上传软件包。

Release 包发布完成后，组织维护者将软件信息更新到 **[oheco-packages](https://github.com/oheco/oheco-packages)**，包括维护者、版本、支持平台、下载地址、文件大小和 SHA-256 等。软件索引和下载站更新后，所有用户即可浏览、下载或通过 `oo` 安装该软件。

用户安装新收录软件的方式为（将 `<包名>` 替换为实际名称）：

```text
oo update
oo info <包名>
oo install <包名>
```

打包格式与索引字段详见 [oheco-packages 包规范](https://github.com/oheco/oheco-packages#readme)。发行包先发布，再更新索引；后续适配修订使用新版本，保留已发布版本的产物。

## 发布后的维护

项目维护者继续跟进用户问题、上游更新和系统兼容性。需要发布新版本时，明确新的上游 Release 或适配修订版本，完成适配与验证后，再通知组织维护者打包和更新软件目录。

当前项目维护者名单见[组织主页中的维护者列表](https://github.com/oheco/.github/blob/main/profile/README.md#维护者)。
